# 2026 layout

## Status

Detailed planting diagram not yet added. Next step: trace or draw `beds.svg` and add `beds-legend.md` per [CONVENTIONS.md](../../CONVENTIONS.md).

## Dimensions (plan of record)

| Structure | Interior size |
|-----------|----------------|
| Raised boxes (default) | 6 ft × 3 ft each |
| Greenhouse | 6 ft × 4 ft |

**Also diagram when you have positions:** Back-yard **peach tree**, **cherry shrub**, and **compost bin** (compost on drip — show emitter and which **timer/circuit** matches [CONVENTIONS.md](../../CONVENTIONS.md)). Front yard vining area may need hand-watering or a future drip circuit. Indoor pots are out of scope for `beds.svg` unless you want a separate indoor sketch.

## Units

State the SVG unit (e.g. inches or cm) here when the file exists; interior bed sizes should match the table above.

## North arrow

Add a north arrow policy when you publish the SVG (which way is “up” on the page vs. true north).
